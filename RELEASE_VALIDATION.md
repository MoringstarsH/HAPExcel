# 发布稳定性验收记录

更新时间：2026-08-18

## 自动化基线

- `npm test`：15 个测试文件、82 项测试全部通过。
- `mdye build`：构建成功，生产 bundle 343 KiB；只有既有体积阈值告警。
- 大数据渲染策略：超过 300 个逻辑行后启用窗口化，500、2000、10000 行场景的自动化测试要求 DOM 行窗口不超过 80 行。
- 发布操作：未执行 `mdye push`。

## 真实 HAP iframe 验收

当前状态：待连接已登录的 HAP 测试页面。应用内浏览器中没有可定位的 HAP 标签页或历史地址，不能安全猜测租户 URL。

连接页面后按以下顺序执行，测试记录统一使用 `STABILITY-TEST-<时间戳>` 标识：

1. 确认页面无用户遗留草稿，检查诊断面板无未解释错误。
2. 新增一条测试记录并保存，刷新后核对服务端值和记录 ID。
3. 修改该记录并保存，刷新后核对修改值及计算/只读字段。
4. 新增一条合法记录并制造另一条业务失败，确认成功项不再次提交、失败草稿保留、删除暂缓。
5. 删除测试记录并保存，刷新后确认记录不存在；清理测试数据前需按浏览器安全要求再次确认。
6. 保存过程中模拟断网，确认新增结果未知时不会自动重发；恢复网络后刷新并人工核对。
7. 验证存在草稿时刷新、筛选变化、关闭页面均不会静默丢失草稿。
8. 在 iframe 中向上、向下拖动填充柄到滚动边缘，确认自动滚动、预览、松手提交和取消行为。
9. 在总量 500、2000、10000 的测试数据集上记录首屏、交互延迟、长任务、滚动和内存数据。

## 发布门槛

- 首屏加载不超过 2 秒。
- 常用选择、编辑操作不超过 100ms。
- 持续滚动无明显长卡顿，诊断日志中不存在未解释的运行时错误。
- 新增、修改、删除、部分失败、断网恢复及离页保护全部通过。
- 所有测试数据清理完成后，再单独决定是否执行发布。
