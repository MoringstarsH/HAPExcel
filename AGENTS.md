# 原材料进场台账自定义视图

## 项目目标

本项目是明道云 HAP「原材料进场台账」工作表的自定义表格视图。目标不是制作一个只能展示数据的列表，而是提供接近飞书多维表格和电子表格的批量录入体验，同时保留明道云字段类型、权限、关联选择器和数据接口能力。

相关标识：

- 工作表 ID：`6a7ed7186e0668db12749ad9`
- 自定义视图 ID：`6a7ed7425715a1412e4e74b7`
- 项目目录：`mdye_view_6a7ed7425715a1412e4e74b7`

## 核心需求

1. 表格必须可选择、可编辑、可新增、可批量修改，不能退化为只读数据展示。
2. 支持连续矩形选区、鼠标拖拽、Shift 扩展和方向键移动。
3. 支持 Ctrl/Cmd+C、Ctrl/Cmd+V；单个值粘贴到多格选区时填充全部选中单元格。
4. 复制时同时生成标准 TSV 文本和包含字段 ID、类型、原始值的内部结构化剪贴板。
5. 文本、数字、日期等普通字段按目标字段类型解析和校验。
6. 关联、成员和选项字段在本视图内部复制时保留 ID/key：
   - 粘贴回同一特殊字段时直接恢复原始值。
   - 粘贴到文本字段时使用标题、姓名或选项名称。
   - 粘贴到数字、日期、不同关联字段或不同选项字段时标记类型不匹配。
   - 外部纯文本不得自动猜测关联记录或成员，避免重名误关联。
7. 合法单元格可以形成草稿；不合法和只读单元格保留原值并显示错误。存在错误时禁止保存。
8. 保存前必须显示新增、修改、删除和错误数量。删除只在保存确认后提交服务端。
9. 行首复选框负责整行删除选择；单元格矩形选区是独立状态，两者不得混用。
10. 筛选或外部数据变化时，如存在本地草稿，不得直接覆盖草稿。
11. Type 29 关联记录必须以浅蓝色标签显示标题，悬停呈链接效果并可打开关联记录；选择后、保存前以及恢复草稿时都不得把记录 ID/UUID 暴露为显示文本。

## 当前实现状态

### 已完成

- 使用原生 DOM 表格替换 Canvas 表格，解决明道云 iframe 内无法选中和编辑的问题。
- 单击选择活动单元格；拖拽形成连续矩形选区；支持反向拖拽。
- 支持 Shift+单击、Shift+方向键扩展选区。
- 支持方向键移动，Enter/F2 编辑，Esc 取消，Enter/Tab 提交并移动。
- 编辑器仅在进入编辑状态后挂载，普通选择不会被浏览器 input 行为拦截。
- 支持纯文本 TSV 和内部结构化剪贴板的复制、粘贴。
- 支持单值填充多格选区和二维矩阵粘贴。
- 粘贴最多 5000 个单元格，单次最多自动新增 200 行。
- 支持文本、数字、金额、日期、日期时间、时间、复选框、单选、多选、成员和关联字段适配。
- 特殊字段遵守“同字段保留 ID、文本字段降级为名称、错误类型拒绝”的规则。
- 类型错误、只读字段和校验错误进入单元格错误状态并阻止保存。
- 支持新增行、整行待删除、撤销删除、草稿自动保存与恢复、放弃草稿。
- 保存支持新增、更新和删除；部分失败时保留失败草稿，可重试。
- 支持分页加载、筛选更新和外部新增记录事件。
- Type 29 关联记录使用浅蓝色标签展示，悬停呈链接效果，点击可通过明道原生记录弹窗打开目标记录。
- 关联选择器返回记录后立即按目标表标题字段标准化为 `{ sid, name }`，未保存状态直接显示用户选中的标题。
- 关联标题字段遵循 HAP 原生规则：优先使用 `advancedSetting.showtitleid`，否则使用目标工作表中 `attribute === 1` 的字段。
- ID-only 历史草稿或缺少标题的关联值会通过目标记录详情异步补齐；加载中和失败状态均不显示 UUID。
- 关联记录的草稿状态保留 `{ sid, name }` 供界面展示，提交服务端时仍只序列化 `{ sid }`，不改变 HAP 保存协议。
- 已移除不再使用的 `@glideapps/glide-data-grid` 依赖。

### 已验证

- 自动化测试：6 个测试文件、20 项测试全部通过。
- `mdye build` 构建成功，生产 bundle 约 243 KiB。
- 明道云实际 iframe 验证：
  - 鼠标拖拽成功选择 4 个连续单元格。
  - 单格复制后粘贴到 3 格选区，3 行正确形成修改草稿。
  - 剪贴板同时包含 `text/plain` 和内部结构化数据。
  - Shift+方向键成功扩展为 3 格选区。
  - 数值字段 Enter 进入编辑，Esc 退出且不产生草稿。
  - 验收产生的草稿已全部放弃，未向线上保存测试数据。

### 尚未完成或明确不在第一版范围

- 不支持 Ctrl/Cmd 多块不连续选区。
- 不支持填充柄和数字、日期序列推演。
- 外部纯文本不自动查询匹配成员或关联记录。
- 多条关联字段若服务端仅返回数量，当前展示仍依赖现有返回值；需要完整标题时应通过 `getRowRelationRows` 按需加载。
- 尚未执行 `mdye push`，当前改动只在本地调试和构建产物中。
- 尚未进行大数据量下的长期性能和内存压力测试。

## 代码结构

- `src/App.js`：页面状态、DOM 表格、编辑模式、选择和快捷键接线。
- `src/selection.js`：单元格坐标、矩形范围、移动和扩展计算。
- `src/clipboard.js`：TSV、结构化剪贴板及跨字段粘贴映射。
- `src/adapters.js`：明道字段类型的显示、解析、校验和序列化。
- `src/rows.js`：服务端行、草稿行、修改、删除和合并状态。
- `src/drafts.js`：本地草稿持久化、版本与字段结构兼容处理。
- `src/commit.js`：新增、更新、删除的批量提交和部分失败处理。
- `src/gateway.js`：明道 `mdye` API 与原生字段选择器封装。
- `src/style.less`：工具栏、DOM 表格、选区、活动格和错误状态样式。

## 实现约束

- 页面运行在明道云的 iframe 中。不要依赖顶层页面坐标或跨 iframe 的 Canvas 命中。
- 不要重新引入 Canvas 数据网格，除非已经在真实明道 iframe 中证明鼠标、键盘和编辑器事件全部可靠。
- 字段是否可写必须同时考虑控件类型、`readonly`、`disabled` 和记录删除状态。
- 公式、汇总、自动编号、查找等只读字段不得因批量粘贴被修改。
- 任何批量操作都必须先通过 adapter 转换，不能直接把显示文本写入服务端字段。
- 成员和关联记录不得仅凭名称静默匹配。
- 关联记录 ID 只能用于提交和打开目标记录，禁止作为标签显示文本的兜底值。
- 解析 type 29 标题时不得假定选择结果固定包含 `name` 或 `title`；必须根据 `showtitleid`/`attribute === 1` 定位动态字段，并缓存目标工作表元数据。
- 异步补齐关联标题不得把原本干净的服务端记录误标为已修改，也不得改变关联字段最终 `{ sid }` 的提交格式。
- 不要在验收时点击“保存”。测试应形成草稿后立即放弃，并先确认不存在用户已有草稿。
- 发布属于独立操作；除非用户明确要求，不执行 `mdye push`。

## 继续开发时的验证顺序

1. 运行 `npm test`。
2. 运行 `mdye build`。
3. 在明道云调试视图中验证单击、拖拽、Shift 扩展和键盘导航。
4. 验证“复制一格 → 选择多格 → 粘贴填充”。
5. 分别验证普通字段、同字段特殊类型、跨类型错误和只读字段。
6. 检查草稿统计、错误状态和保存禁用条件。
7. 验证 type 29 选择后立即显示标题、历史 ID-only 草稿自动补齐、标签可点击且保存载荷仍仅包含 `sid`。
8. 放弃所有验收草稿，确认数据恢复且保存按钮禁用。

## 完成度

- 基础加载与展示：100%
- 单格选择与编辑：100%
- 连续矩形选择：100%
- 常用键盘操作：90%
- 普通字段批量粘贴：100%
- 内部特殊字段复制粘贴：90%
- 草稿、校验和保存：90%
- 大数据量性能验证：30%
- 正式发布：0%

当前整体完成度约 **85%**。达到可发布状态前，建议补充特殊字段的真实页面验收、保存接口的测试环境端到端验证，以及至少 500 行数据下的选择和粘贴性能测试。
