# 原材料进场台账自定义视图

## 项目目标

本项目是明道云 HAP「原材料进场台账」工作表的自定义表格视图。目标不是制作一个只能展示数据的列表，而是提供接近飞书多维表格和电子表格的批量录入体验，同时保留明道云字段类型、权限、关联选择器和数据接口能力。

相关标识：

- 工作表 ID：`6a7ed7186e0668db12749ad9`
- 自定义视图 ID：`6a7ed7425715a1412e4e74b7`
- 项目目录：`mdye_view_6a7ed7425715a1412e4e74b7`

## 核心需求

1. 表格必须可选择、可编辑、可新增、可批量修改，不能退化为只读数据展示。
2. 支持连续矩形选区、鼠标拖拽、Shift 扩展和方向键移动。
3. 支持 Ctrl/Cmd+C、Ctrl/Cmd+V；单个值粘贴到多格选区时填充全部选中单元格。
4. 复制时同时生成标准 TSV 文本和包含字段 ID、类型、原始值的内部结构化剪贴板。
5. 文本、数字、日期等普通字段按目标字段类型解析和校验。
6. 关联、成员和选项字段在本视图内部复制时保留 ID/key：
   - 粘贴回同一特殊字段时直接恢复原始值。
   - 粘贴到文本字段时使用标题、姓名或选项名称。
   - 粘贴到数字、日期、不同关联字段或不同选项字段时标记类型不匹配。
   - 外部纯文本不得自动猜测关联记录或成员，避免重名误关联。
7. 合法单元格可以形成草稿；不合法和只读单元格保留原值并显示错误。存在错误时禁止保存。
8. 保存前必须显示新增、修改、删除和错误数量。删除只在保存确认后提交服务端。
9. 行首复选框负责整行删除选择；单元格矩形选区是独立状态，两者不得混用。
10. 筛选或外部数据变化时，如存在本地草稿，不得直接覆盖草稿。
11. Type 29 关联记录必须以浅蓝色标签显示标题，悬停呈链接效果并可打开关联记录；选择后、保存前以及恢复草稿时都不得把记录 ID/UUID 暴露为显示文本。
12. Type 9/10/11 选项字段必须使用彩色标签显示；颜色优先使用 HAP 选项配置，选项 key、复制粘贴和提交载荷保持不变。
13. 自定义视图必须支持通过 HAP 原生插件参数选择显示字段并调整列顺序；没有配置时默认显示业务字段，不显示系统字段和工作流字段。

## 当前实现状态

### 已完成

- 使用原生 DOM 表格替换 Canvas 表格，解决明道云 iframe 内无法选中和编辑的问题。
- 单击选择活动单元格；拖拽形成连续矩形选区；支持反向拖拽。
- 支持 Shift+单击、Shift+方向键扩展选区。
- 支持方向键移动，Enter/F2 编辑，Esc 取消，Enter/Tab 提交并移动。
- 编辑器仅在进入编辑状态后挂载，普通选择不会被浏览器 input 行为拦截。
- 支持纯文本 TSV 和内部结构化剪贴板的复制、粘贴。
- 支持单值填充多格选区和二维矩阵粘贴。
- 粘贴最多 5000 个单元格，单次最多自动新增 200 行。
- 支持文本、数字、金额、日期、日期时间、时间、复选框、单选、多选、成员和关联字段适配。
- 特殊字段遵守“同字段保留 ID、文本字段降级为名称、错误类型拒绝”的规则。
- 类型错误、只读字段和校验错误进入单元格错误状态并阻止保存。
- 支持新增行、整行待删除、撤销删除、草稿自动保存与恢复、放弃草稿。
- 保存支持新增、更新和删除；部分失败时保留失败草稿，可重试。
- 支持分页加载、筛选更新和外部新增记录事件。
- Type 29 关联记录使用浅蓝色标签展示，悬停呈链接效果，点击可通过明道原生记录弹窗打开目标记录。
- 关联选择器返回记录后立即按目标表标题字段标准化为 `{ sid, name }`，未保存状态直接显示用户选中的标题。
- 关联标题字段遵循 HAP 原生规则：优先使用 `advancedSetting.showtitleid`，否则使用目标工作表中 `attribute === 1` 的字段。
- ID-only 历史草稿或缺少标题的关联值会通过目标记录详情异步补齐；加载中和失败状态均不显示 UUID。
- 关联记录的草稿状态保留 `{ sid, name }` 供界面展示，提交服务端时仍只序列化 `{ sid }`，不改变 HAP 保存协议。
- 已移除不再使用的 `@glideapps/glide-data-grid` 依赖。
- 单元格选区使用蓝色边框：单格显示完整边框，多格只显示矩形外轮廓，不使用蓝色填充。
- 匹配错误单元格使用 `#fdcaca` 浅红背景，不再显示红色边框；选中错误单元格时保留蓝色选区边框。
- 只读、不可编辑字段使用 `#f5f6f7` 灰色背景，且悬停时不被普通行悬停色覆盖。
- 单选、多选和下拉单选使用彩色圆角标签显示；多选按原始 key 顺序显示多个标签。
- 选项颜色由 `src/adapters.js` 统一解析：优先读取选项颜色配置，兼容颜色字符串、十六进制颜色和颜色索引；缺少颜色时使用稳定默认色，未知 key 使用中性灰色。
- 选项颜色关闭时使用中性灰色标签；视觉颜色只作用于展示，不参与字段值比较、校验或保存。
- 已在 HAP「插件设置」中建立字段选择器参数“显示字段”，变量 ID 为 `showFields`，允许多选和拖拽排序，并开放全部字段类型。
- 已在当前视图「参数映射」中默认启用 18 个业务字段，保留 15 个系统/工作流字段为隐藏状态。
- 表格按 `env.showFields` 的字段 ID 顺序渲染列；配置为空时回退到业务字段，已删除或不可用的字段 ID 会被忽略。
- 显示字段只影响界面列：隐藏字段仍保留在完整记录、草稿、校验和提交链路中，避免切换显示列导致数据或草稿结构损坏。
- 隐藏字段存在校验错误时会提示具体字段名称，引导用户重新显示该字段后修正。
- 支持 Excel 风格填充柄：当前选区右下角显示小方块，可向上或向下拖拽批量复制；单格复制单值，多格按源选区顺序循环复制。
- 填充柄只对全部可编辑的选区显示；默认覆盖目标值，但只写入本地草稿，支持现有撤销、放弃草稿和保存校验流程。
- 填充预览在松手前不修改草稿；拖到末尾可临时预览并自动新增最多 200 行草稿，超出单次单元格或新增行上限时整次拒绝。
- 文本、数字、日期、复选框、选项、成员和关联字段均支持填充；特殊字段通过 adapter 深拷贝保留 key、账号 ID、关联 `sid` 和标题元数据，不按名称重新匹配。
- 支持 Excel 风格布局操作：列宽可拖拽调整、行高可按记录独立调整，尺寸有最小/最大边界。
- 布局配置写入视图 `advancedSetting.hapExcelLayout`，该命名空间值必须使用 JSON 字符串格式，并保留其他高级配置。
- 布局保存通过 `apis.worksheet.saveWorksheetView` 调用，并在 `editAdKeys` 中声明 `hapExcelLayout`，确保 HAP 后端持久化该配置。
- 布局保存采用 500ms 防抖；视图切换或插件卸载前会冲刷未完成的布局保存，避免只在刷新后才看到更新。
- 旧的对象格式布局可正常读取，并会在首次加载时自动迁移为 JSON 字符串格式。

### 已验证

- 自动化测试：12 个测试文件、59 项测试全部通过。
- `mdye build` 构建成功，生产 bundle 约 298 KiB；存在项目既有的 bundle 体积告警，不影响构建结果。
- 明道云实际 iframe 验证：
  - 鼠标拖拽成功选择 4 个连续单元格。
  - 单格复制后粘贴到 3 格选区，3 行正确形成修改草稿。
  - 剪贴板同时包含 `text/plain` 和内部结构化数据。
  - Shift+方向键成功扩展为 3 格选区。
- 数值字段 Enter 进入编辑，Esc 退出且不产生草稿。
- 填充逻辑自动化测试覆盖单格、多格循环、多列、上下方向、边界新增、上限拒绝和特殊字段深拷贝。
- 填充柄真实 HAP iframe 的鼠标视觉、自动滚动和端到端保存尚未验收。
- 验收产生的草稿已全部放弃，未向线上保存测试数据。

### 尚未完成或明确不在第一版范围

- 不支持 Ctrl/Cmd 多块不连续选区。
- 不支持数字、日期序列推演；填充柄当前只执行复制，不推算序列。
- 外部纯文本不自动查询匹配成员或关联记录。
- 多条关联字段若服务端仅返回数量，当前展示仍依赖现有返回值；需要完整标题时应通过 `getRowRelationRows` 按需加载。
- 尚未执行 `mdye push`，当前改动只在本地调试和构建产物中。
- 尚未进行大数据量下的长期性能和内存压力测试。

## 代码结构

- `src/App.js`：页面状态、DOM 表格、编辑模式、选择和快捷键接线。
- `src/selection.js`：单元格坐标、矩形范围、移动和扩展计算。
- `src/clipboard.js`：TSV、结构化剪贴板及跨字段粘贴映射。
- `src/adapters.js`：明道字段类型的显示、解析、校验和序列化。
- `src/rows.js`：服务端行、草稿行、修改、删除和合并状态。
- `src/drafts.js`：本地草稿持久化、版本与字段结构兼容处理。
- `src/commit.js`：新增、更新、删除的批量提交和部分失败处理。
- `src/gateway.js`：明道 `mdye` API、视图布局保存与原生字段选择器封装。
- `src/columns.js`：视图显示字段解析、字段可见性回退和隐藏字段错误提示。
- `src/query.js`：HAP 原生排序、筛选参数构造及外部筛选合并。
- `src/layout.js`：列宽、行高边界处理、布局读写、旧格式迁移和 HAP 高级配置序列化。
- `src/fill.js`：选区拖拽填充计算与预览。
- `src/fill.test.js`：拖拽填充规则、边界和特殊字段复制测试。
- `src/style.less`：工具栏、DOM 表格、选区、活动格和错误状态样式。

## 显示字段配置

### 使用方式

1. 打开自定义视图右侧菜单中的「配置视图」。
2. 进入「参数映射」，展开「显示字段」。
3. 使用开关把字段移入「显示」或「隐藏」区域；拖拽「显示」区域中的字段可调整表格列顺序。
4. 配置会写入运行时 `env.showFields` 并自动应用，无需在插件代码中维护字段 ID。

### HAP 原生参数定义

- 参数位置：「插件设置」→「参数设置」。
- 类型：字段选择器。
- 名称：`显示字段`。
- 变量 ID：`showFields`。
- 允许选择数量：多选。
- 允许选择的字段类型：全部；如果此项为空，「参数映射」会显示 0 个可选字段。

### 实现约束

- `src/columns.js` 统一解析字符串或字段对象形式的参数值，并按参数数组顺序返回可见字段。
- `view.controls` 中被 HAP 隐藏的字段和 type 22 分段字段不得进入可见列。
- 默认业务字段回退会排除记录 ID、拥有者、创建/修改信息及 `wf*` 工作流字段。
- 加载、分页合并、草稿结构哈希、关联标题补齐、完整校验和提交必须继续使用 `allControls`/`allAdapters`；只有表格展示、选择、复制粘贴和编辑定位使用可见 `controls`/`adapters`。

## 视觉状态与选项标签实现方法

### 单元格选区

- `src/App.js` 使用 `selectionRange` 计算当前矩形选区的上下左右边界。
- 每个选中单元格只添加对应边缘类名：`cell-selection-top`、`cell-selection-right`、`cell-selection-bottom`、`cell-selection-left`。
- `src/style.less` 通过 `td.cell-selected::after` 绘制边框，单格自然形成完整边框，多格形成统一外轮廓。
- 选区不改变单元格背景；错误背景、只读背景和悬停状态通过更高优先级规则保持各自语义。

### 单选/多选标签

- `src/adapters.js` 的 `optionPresentation(control, key)` 负责将选项 key 转换为 `{ key, label, color, colored }`。
- `createFieldAdapter` 提供 `optionTag` 和 `optionTags`，表格展示和选择弹窗共用同一套解析结果。
- `src/App.js` 的 `OptionTags` 只负责渲染标签，不改变原始值；单选渲染一个标签，多选渲染多个标签。
- 选项颜色直接作为 `--option-color` 使用，避免再次与白色混合造成颜色过淡；标签文字使用深色以保证可读性。
- 颜色缺失时按选项位置使用稳定默认调色板；未知选项 key 不丢失，使用灰色标签显示。
- 单选、多选仍通过原有选择弹窗编辑；保存时继续序列化为 key 数组。

## 实现约束

- 页面运行在明道云的 iframe 中。不要依赖顶层页面坐标或跨 iframe 的 Canvas 命中。
- 不要重新引入 Canvas 数据网格，除非已经在真实明道 iframe 中证明鼠标、键盘和编辑器事件全部可靠。
- 字段是否可写必须同时考虑控件类型、`readonly`、`disabled` 和记录删除状态。
- 公式、汇总、自动编号、查找等只读字段不得因批量粘贴被修改。
- 任何批量操作都必须先通过 adapter 转换，不能直接把显示文本写入服务端字段。
- 成员和关联记录不得仅凭名称静默匹配。
- 关联记录 ID 只能用于提交和打开目标记录，禁止作为标签显示文本的兜底值。
- 解析 type 29 标题时不得假定选择结果固定包含 `name` 或 `title`；必须根据 `showtitleid`/`attribute === 1` 定位动态字段，并缓存目标工作表元数据。
- 异步补齐关联标题不得把原本干净的服务端记录误标为已修改，也不得改变关联字段最终 `{ sid }` 的提交格式。
- 不要在验收时点击“保存”。测试应形成草稿后立即放弃，并先确认不存在用户已有草稿。
- `advancedSetting.hapExcelLayout` 必须保存为 JSON 字符串；调用 `saveWorksheetView` 时必须在 `editAdKeys` 中包含 `hapExcelLayout`。
- 布局保存不能只依赖防抖定时器；组件卸载或切换视图前必须提交仍处于待保存状态的最新布局。
- 发布属于独立操作；除非用户明确要求，不执行 `mdye push`。

## 继续开发时的验证顺序

1. 运行 `npm test`，确认当前 12 个测试文件、59 项测试全部通过。
2. 运行 `mdye build`，确认只保留既有 bundle 体积告警。
3. 在明道云调试视图中验证单击、拖拽、Shift 扩展和键盘导航。
4. 验证“复制一格 → 选择多格 → 粘贴填充”。
5. 验证填充柄可向上/下拖拽，单格复制、多格循环、多列复制和拖回源范围取消。
6. 验证填充预览不产生草稿，松手后一次性生成修改并可 Ctrl/Cmd+Z 撤销。
7. 验证拖到末尾的临时预览、自动新增草稿、200 行上限和 5000 单元格上限。
8. 分别验证普通字段、同字段特殊类型、跨类型错误和只读字段。
9. 检查草稿统计、错误状态和保存禁用条件。
10. 验证 type 29 选择后立即显示标题、历史 ID-only 草稿自动补齐、标签可点击且保存载荷仍仅包含 `sid`。
11. 验证单选、多选标签颜色、多个标签排列、颜色关闭和未知 key 降级。
12. 验证单格蓝色完整边框、多格矩形外轮廓、错误 `#fdcaca` 背景和只读灰色背景。
13. 验证「参数映射 → 显示字段」能列出字段，切换显示状态与拖拽排序后表格列和 `env.showFields` 顺序一致。
14. 验证隐藏字段不破坏现有草稿，隐藏字段校验失败时能看到明确提示。
15. 调整列宽和行高，等待布局状态显示“布局已保存”，刷新后确认恢复。
16. 调整布局后立即切换到其他视图，再切回当前视图，确认最新列宽和行高仍然存在。
17. 验证旧对象格式 `hapExcelLayout` 能读取并自动迁移，且 HAP 不再出现 `advancedSetting.hapExcelLayout` 解析错误。
18. 放弃所有验收草稿，确认数据恢复且保存按钮禁用。

## 完成度

- 基础加载与展示：100%
- 单格选择与编辑：100%
- 连续矩形选择：100%
- 常用键盘操作：90%
- 普通字段批量粘贴：100%
- 内部特殊字段复制粘贴：90%
- 拖拽填充柄：100%（自动化测试已完成，真实 HAP iframe 交互待验收）
- 草稿、校验和保存：90%
- 大数据量性能验证：30%
- 正式发布：0%

当前整体完成度约 **85%**。达到可发布状态前，建议补充特殊字段的真实页面验收、保存接口的测试环境端到端验证，以及至少 500 行数据下的选择和粘贴性能测试。
